# Packages ---------------------------------------------------------------------

# Data Wrangling ---------------------------------------------------------------

library(tidyverse) 
library(lubridate) # for dates and times

# Statistical Methods ----------------------------------------------------------

library(boot) # for bootstrapping
library(broom) # for turning data into tidy data

# Python -----------------------------------------------------------------------

library(reticulate) # for Python code

# Reporting --------------------------------------------------------------------

library(kableExtra) # for tables